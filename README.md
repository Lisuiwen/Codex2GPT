# Codex2GPT

Bridge Codex to a user-visible ChatGPT browser session for occasional high-value review tasks.

> Experimental project. The browser adapter is intentionally isolated so the transport can later be replaced by an official API without changing the Codex-side interface.

The initial architecture is being scaffolded in this repository.
